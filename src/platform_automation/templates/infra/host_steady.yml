{{host}}:
    # Tailscale MagicDNS name, not a public address.
    ansible_host: {{tailnet}}
    ansible_user: ops
    ansible_ssh_private_key_file: {{ssh_key}}
    ansible_port: 22
    platform_public_interface: {{interface}}
