{{host}}:
    # Public address from the provider.
    ansible_host: {{public_address}}
    ansible_user: root
    # Provider account. The key is installed manually once,
    # before the first run; see docs/BOOTSTRAP.md.
    ansible_ssh_private_key_file: {{ssh_key}}
    ansible_port: 22
    platform_public_interface: {{interface}}
